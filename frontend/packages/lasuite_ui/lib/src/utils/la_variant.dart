/// The semantic variant of a feedback-oriented component (alert, toast,
/// field validation state, ...). Mirrors the upstream `VariantType` enum.
enum LaVariant { info, success, warning, error, neutral }
