/// Shared max width for page content on desktop, so Proposals, History and
/// Settings all read as the same column width instead of each screen
/// capping independently.
const double kPageContentMaxWidth = 1280;
